NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# OpenWeatherMap API Key for weather map tiles (optional: falls back to transparent tiles if unconfigured)
OPENWEATHER_API_KEY=
NEXT_PUBLIC_OPENWEATHER_API_KEY=

# Groq API Key for AI chat and persona insights (optional: falls back to deterministic engine if unconfigured)
GROQ_API_KEY=
